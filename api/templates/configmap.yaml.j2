apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ trader_name }}-config
  namespace: {{ namespace }}
  labels:
    app: hyper-trader
    trader-address: "{{ wallet_address }}"
    managed-by: hyper-trader-api
data:
  config.json: |
{{ config_json | indent(4, first=True) }}
