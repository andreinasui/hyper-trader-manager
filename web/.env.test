# Test Environment Variables
# These are used by the Vite dev server during Playwright tests

# Mock Privy App ID (not used in tests, but prevents errors)
VITE_PRIVY_APP_ID=test_mock_privy_app_id

# API URL (mocked in tests)
VITE_API_URL=http://localhost:8000
