# Runtime-written files — overwritten by the API and Traefik at startup.
# Only the seed config (dynamic/00-bootstrap.yml) belongs in version control.
acme.json
certs/
dynamic/*
!dynamic/00-bootstrap.yml
