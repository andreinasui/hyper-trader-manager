# Initial Traefik config - HTTP only
# This will be overwritten by SSL setup wizard
entryPoints:
  web:
    address: ":80"

# Enable ping endpoint for healthcheck
ping: {}

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true
